on: push

name: Jira Example

jobs:
  build:
    runs-on: ubuntu-latest
    name: Jira Example
    steps:
  
    - name: TEST
      run: |
        example-commend ${{ secrets.JIRA_BASE_URL }}
        
